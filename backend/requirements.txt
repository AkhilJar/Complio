fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic-settings==2.6.1
alembic==1.14.0
#s3 client — minio is s3-compatible, so the same code works against real s3 later
boto3==1.35.54
pytest==8.3.3
#html-to-text for bill text extraction
beautifulsoup4==4.12.3
#mechanical pdf-to-markdown extraction for uploaded documents — no model call
pymupdf4llm==0.0.17
#fastapi needs this to parse multipart form data (the pdf upload)
python-multipart==0.0.12
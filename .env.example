POSTGRES_USER=complio_user
POSTGRES_PASSWORD=changeme
POSTGRES_DB=complio

#minio root credentials — also used as the s3 access key / secret key
#password must be at least 8 characters or minio refuses to start
MINIO_ROOT_USER=complio_minio
MINIO_ROOT_PASSWORD=changeme123